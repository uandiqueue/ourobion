# empty_insights_seedpod Review

- Status: accepted
- Background mode: transparent
- Final asset:
  `apps/biotope/assets/images/generated/biomech_botanical/empty_states/empty_insights_seedpod.png`
- Candidate attempt:
  `docs/biotope/ui/ai-assets/reviews/candidates/empty_insights_seedpod_attempt_1.png`

## UI Seed Fit

The closed pale-sage pod, two new leaves, porcelain seam, and small brass nodes
communicate dormant growth without resembling a brain, anatomical organ, alien
egg, or circuit component.

## Background Mode

Correct. The final is a 1024 x 1536 `Format32bppArgb` PNG with all corner alpha
values at 0. The audit found 1,419,062 transparent pixels, 3,534 partial edge
pixels, 150,268 opaque pixels, zero blue-fringe pixels, and inset bounds of
`(192, 252)` to `(789, 1322)`.

## Slightly Off

None material. Its simple silhouette is appropriate for an Insights-not-ready
state and remains distinct from the flower-based assets.

## Flutter Usage

Use when insights are not ready yet.
