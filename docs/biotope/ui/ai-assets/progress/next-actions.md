# Next Actions

All 25 asset IDs in `asset-manifest.json` are accepted. No generation work is
currently queued.

Before adding more generated assets, define new asset IDs, Flutter usage,
category, output paths, and background modes in the manifest. Do not regenerate
accepted assets unless a specific visual or integration review identifies a
problem.
