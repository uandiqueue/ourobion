---
title: R4-U1 reconciled — re-advanced RUN4_UNIT_BASE_SHA past #199, attestation re-recorded
summary: PR #232's green status went stale a second time when #199 (U4 scientific-semantics/trust-labels) merged after the branch's prior base (da6b11b) was set; merged the current dev-phase2-run4 tip into ci/run4-u1-reconciled, re-advanced RUN4_UNIT_BASE_SHA to 789e6a0 in lockstep across the gate tool and ci.yml, re-recorded the local runtime attestation through the generator, and re-proved the gate fails closed against the new base with four injected negatives.
type: session
scope: shared
status: canonical
updated: 2026-07-28
---

# R4-U1 reconciled — base re-advance past #199

Branch: `ci/run4-u1-reconciled` · PR #232 · supersedes #170, #180

## Attempted

Mechanical continuation of the #232 landing procedure (already run twice today for earlier base
drifts). Verified the trigger first rather than trusting the brief: `git rev-parse
origin/dev-phase2-run4` was `789e6a0` (PR #199, the U4 scientific-semantics/trust-labels unit),
one merge ahead of the `da6b11b` base #232 was last measured against. Cumulative delta from the
much older `2749381` base was ~12,500 added lines against the 8,500 cap, so the landing check would
fail on #232 until the base moved again.

## Changed

- **Merged `origin/dev-phase2-run4` into `ci/run4-u1-reconciled`** (merge, not rebase — the branch
  is pushed and referenced by #232). One real conflict, in `supabase/deploy-attestation.json`
  (the `generate-insights` function's recorded hashes — expected, since both this PR's auth changes
  and #199's trust-label changes touch that file). Resolved via `git checkout --ours` rather than
  hand-editing the JSON, since the whole manifest gets rewritten by the attestation generator in the
  next step regardless of which side "wins" the conflict resolution. `generate-insights/index.ts`
  itself auto-merged cleanly with no conflict markers; verified after the merge that it carries
  **both** sets of changes — this PR's `verifyInternalSecretRequest`/`readServerKeyEnv`/
  `resolveServerKey` auth path and #199's `trustFailures`/`shared/brain/trust_labels.ts` artifact-
  trust gate — and that all three other function entrypoints still call
  `verifyInternalSecretRequest`.
- **`RUN4_UNIT_BASE_SHA` advanced `da6b11b` → `789e6a0`** in both `tools/run4_release_gate.mjs` and
  `.github/workflows/ci.yml`, in lockstep (the gate fails closed with "run4-release landing
  constants drifted" if they disagree — verified this is still armed, see Verification). Recorded
  the superseded value and the reason inline, following the existing comment convention. Caps
  unchanged at 115 / 8,500.
- **Re-recorded `supabase/deploy-attestation.json` through the generator** — never hand-edited.
  Served the four functions locally (`node_modules/supabase/bin/supabase.exe functions serve
  --debug --no-verify-jwt`), probed all four routes with no secret configured (genuine 401,
  identical body hash across all four — the designed no-oracle property), generated frozen module
  graphs via `graph-hashes`, then ran `record-attestation --routes-base64 ...`. Diff is confined to
  the `unitBaseSha` provenance line plus `generate-insights`'s `entrypointSha256` /
  `moduleGraphSha256` (its content changed from the merge); `scope: local-only` and
  `hostedDeployParityClaimed: false` are unchanged. Stopped and removed the
  `supabase_edge_runtime_ourobion` container the probe created; left the four pre-existing
  Supabase stack containers untouched.

## Decided

- Resolved the attestation merge conflict with `git checkout --ours` instead of an inline edit,
  since the file's content is fully regenerated by the trusted generator immediately afterward —
  hand-authoring a plausible-looking interim value would have been the exact hand-edit the
  procedure forbids, even though it was never going to be the committed value.
- Did not touch the security content of this PR: `tools/secret-scan/allowlist.json`,
  `gitleaks.toml`, `pins.json`, `pin.mjs`, `secret_scan_guard*.mjs`,
  `check_arch_boundaries*.mjs`, or any scanner step in `ci.yml` beyond the base-SHA line.

## Verification

Real output, all from this branch (base `789e6a0ff8232057402e1d34583647349c85bb89`):

- `node tools/run4_release_gate.mjs config` — `run4 config/workflow gate: PASS`
- `node --test tools/run4_release_gate.test.mjs` — 12/12 pass
- `node tools/run4_release_gate.mjs attest --deno <deno 2.8.1> --supabase-cli
  node_modules/supabase/bin/supabase.exe --graph-dir <tmp>` — `run4 local runtime attestation: PASS`
- `node tools/run4_release_gate.mjs landing --base 789e6a0... --head HEAD --max-paths 115
  --max-added 8500` — `{"changedPaths":22,"addedLines":6064}` (well within cap)
- `node --test tools/check_arch_boundaries.test.mjs` — 50/50 pass;
  `node tools/check_arch_boundaries.mjs` — `Scanned 383 source file(s). OK: no
  architecture-boundary violations found.`
- `node --test tools/secret_scan_guard.test.mjs` — 111/111 pass
- `node tools/context_sync.mjs --check` — passed
- **Injected negatives, all against the new base, all failed closed as expected:**
  - stale base (`da6b11b`) → `Error: base must equal accepted current unit SHA 789e6a0...`
  - inflated `--max-added 999999` → `Error: maxAdded must equal accepted cap 8500`
  - inflated `--max-paths 999` → `Error: maxPaths must equal accepted cap 115`
  - a real 9,000-line overcap commit against the correct base/caps →
    `Error: landing delta has 15064 added lines; cap is 8500`, then `git reset --hard` off it
    (never pushed) and re-verified clean

## Left

- Same open items as the prior correction session (product-cap human envelope decision, the
  history-scoping coverage gap, the latent TS/JS alias blind spot, the NUL byte in
  `compute-baselines/index.ts`) — unchanged by this session, not touched here.
- PR left **not merged and not marked ready** per the run owner's standing instruction; they merge.

## Blockers

None. Pushed to the existing branch; #232 remains the single canonical U1 PR.

memory: none
