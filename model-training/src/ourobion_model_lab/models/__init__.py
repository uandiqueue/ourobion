"""Per-model packages for build units MT1-MT5.

Each subpackage below is a placeholder until its own build unit lands; see the
matching docs/temp/model-training/<model>-training-plan.md for the
authoritative scientific specification, and
docs/temp/model-training/code-build-unit-index.md for build status. None of
these register a JobSpec yet -- ourobion_model_lab.job.get_job_class(<name>)
raises ConfigError for all five model names until their unit lands.
"""
