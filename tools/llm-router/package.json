{
  "name": "@ourobion/llm-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ourobion LLM router — dual-route (local-agent mailbox / API-worker) dispatch for every brain-pipeline LLM node, with vendor-family decorrelation and budget enforcement (memory 0013; phase2-run-config C6–C7).",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"tests/**/*.test.ts\"",
    "start": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
