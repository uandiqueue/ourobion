{
  "name": "@ourobion/brain-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ourobion brain-ingestion tool — open-access-first paper-corpus fetcher (discover → resolve → retrieve → extract → manifest).",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"tests/**/*.test.ts\"",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0",
    "fast-xml-parser": "^4.5.0",
    "p-limit": "^6.1.0",
    "unpdf": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
